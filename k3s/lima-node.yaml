# Lima template for a k3s node (server or agent). Parameterized by env vars
# passed on the limactl command line: CPUS, MEMORY, DISK. Attached to Lima's
# `shared` network (socket_vmnet) so the VM, its peers, the keepalived VIP, and
# the Mac all sit on one L2 segment (192.168.105.0/24) — no NAT, no routes.
#
# Deliberately minimal + OFFLINE-friendly: Alpine base (small, fast), no
# containerd/k3s provisioning here — k3s-cluster.sh installs k3s from the
# air-gap bundle after boot. The only provisioning is the host packages k3s and
# our VIP/DNS layer need (keepalived, dnsmasq, curl), which on a truly
# air-gapped host would come from a local apk mirror; see k3s-cluster.sh notes.

vmType: "vz"
vmOpts:
  vz:
    rosetta:
      enabled: true   # lets amd64-only images (IBM MQ) run on Apple Silicon
      binfmt: true

images:
  # Alpine cloud qcow2 with cloud-init (matches Lima's built-in `alpine`
  # template — a plain ISO has no cloud-init and its provision block won't run).
  - location: "https://dl-cdn.alpinelinux.org/alpine/v3.23/releases/cloud/nocloud_alpine-3.23.3-aarch64-uefi-cloudinit-r0.qcow2"
    arch: "aarch64"
    digest: "sha512:7a3cdfaefb0cbf3bb6824cd6ae80d6a3e0b0e367609e5fc50c5f714374d31e8d70dd607094a4e9cfe2e6ead7537781782469fe93bcea79611fbce4ddeacc92e1"
  - location: "https://dl-cdn.alpinelinux.org/alpine/v3.23/releases/cloud/nocloud_alpine-3.23.3-x86_64-uefi-cloudinit-r0.qcow2"
    arch: "x86_64"
    digest: "sha512:9b61666394dcbeb65d3b3ab0864f5ab6c992c32094622333c5b37b83021fd359cc41eb3fa00abf5cdbe25ee2cef600577a7b23eb6f1a103cc32aff862ef88862"

# cpus / memory / disk are set per-VM via `limactl create --cpus/--memory/--disk`.

networks:
  - lima: shared    # socket_vmnet shared net → 192.168.105.0/24

mountType: "virtiofs"
mounts: []          # air-gapped: no host mounts needed; bundle is copied in

containerd:
  system: false
  user: false       # k3s brings its own containerd

# Host packages k3s + keepalived + dnsmasq need. On an internet-connected Mac
# this apk runs at first boot; on a fully air-gapped host, pre-seed a local apk
# repo (or bake a custom image) — k3s-cluster.sh detects a missing package and
# tells you which. keepalived/dnsmasq are only STARTED on the nodes that need
# them (all/server); installing everywhere keeps the image uniform.
provision:
  - mode: system
    script: |
      #!/bin/sh
      set -e
      # Kernel bits k3s needs; harmless if already present.
      modprobe br_netfilter 2>/dev/null || true
      modprobe overlay 2>/dev/null || true
      cat > /etc/sysctl.d/90-k3s.conf <<'EOF'
      net.bridge.bridge-nf-call-iptables = 1
      net.ipv4.ip_forward = 1
      net.ipv4.conf.all.arp_ignore = 0
      net.ipv4.conf.all.arp_announce = 0
      EOF
      sysctl -p /etc/sysctl.d/90-k3s.conf 2>/dev/null || true
      # keepalived needs to bind the VIP that isn't a local iface address.
      echo 'net.ipv4.ip_nonlocal_bind = 1' >> /etc/sysctl.d/90-k3s.conf
      sysctl -w net.ipv4.ip_nonlocal_bind=1 2>/dev/null || true
      if command -v apk >/dev/null 2>&1; then
        apk add --no-cache keepalived dnsmasq curl iproute2 2>/dev/null || \
          echo "WARN: apk add failed (air-gapped without a local mirror?) — install keepalived/dnsmasq/curl by hand" >&2
      fi
  # Shared-net lease WATCHDOG. The #1 laptop failure: the Mac sleeps, the
  # socket_vmnet DHCP lease lapses, lima0 falls back to link-local 169.254.x, and
  # the k3s node goes NotReady (flannel host-gw needs the 192.168.105.x address).
  # This re-acquires the lease automatically within ~1 min of loss, so the node
  # self-heals with no operator action. `scripts/k3s.sh fix-net` is the manual
  # button if this ever can't recover. Idempotent no-op while the lease is held.
  - mode: system
    script: |
      #!/bin/sh
      set -e
      cat > /usr/local/sbin/fix-shared-net <<'SCRIPT'
      #!/bin/sh
      # Re-acquire the shared socket_vmnet (192.168.105.x) DHCP lease on lima0 if
      # it was lost (e.g. after host sleep). No-op while the lease is present.
      IFACE=lima0
      SUBNET=192.168.105
      ip link show "$IFACE" >/dev/null 2>&1 || exit 0
      ip -4 addr show dev "$IFACE" 2>/dev/null | grep -q "inet ${SUBNET}\." && exit 0
      logger -t fix-shared-net "lima0 lost ${SUBNET}.x lease — reacquiring" 2>/dev/null || true
      ip link set "$IFACE" up 2>/dev/null || true
      ifdown "$IFACE" 2>/dev/null || true
      ifup   "$IFACE" 2>/dev/null || true
      # busybox udhcpc fallback: gets a lease and applies it via the default script
      ip -4 addr show dev "$IFACE" 2>/dev/null | grep -q "inet ${SUBNET}\." || \
        udhcpc -i "$IFACE" -n -q -t 6 2>/dev/null || true
      SCRIPT
      chmod +x /usr/local/sbin/fix-shared-net
      # run every minute via busybox crond
      mkdir -p /etc/crontabs
      grep -q '/usr/local/sbin/fix-shared-net' /etc/crontabs/root 2>/dev/null || \
        echo '* * * * * /usr/local/sbin/fix-shared-net' >> /etc/crontabs/root
      rc-update add crond default 2>/dev/null || true
      rc-service crond start 2>/dev/null || true
