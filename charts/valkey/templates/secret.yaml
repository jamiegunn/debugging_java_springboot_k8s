{{- if .Values.auth.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: valkey
  labels: {{- include "valkey.labels" . | nindent 4 }}
type: Opaque
stringData:
  password: {{ .Values.auth.password | quote }}
{{- end }}
