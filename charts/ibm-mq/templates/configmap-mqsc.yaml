apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ibm-mq.fullname" . }}-mqsc
  labels: {{- include "ibm-mq.labels" . | nindent 4 }}
data:
  10-queues.mqsc: |
{{- range .Values.mq.queues }}
    DEFINE QLOCAL('{{ . }}') REPLACE
{{- end }}
    REFRESH SECURITY TYPE(CONNAUTH)
