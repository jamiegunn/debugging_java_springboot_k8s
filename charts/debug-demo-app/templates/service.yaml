apiVersion: v1
kind: Service
metadata:
  name: {{ include "debug-demo-app.fullname" . }}
  labels: {{- include "debug-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "debug-demo-app.selectorLabels" . | nindent 4 }}
